###############################
# File: requirements.txt
# Mô tả: Khai báo thư viện Python cần thiết cho pipeline nhận diện khuôn mặt + web API.
# 
# Files liên quan:
# - backend/main.py (FastAPI server)
# - backend/face_utils.py (Face recognition utilities)
# - backend/database.py (MongoDB connection)
# 
# Lưu ý quan trọng:
# - Kiểm tra lại phiên bản phù hợp với môi trường Python hiện tại (Python 3.8+)
# - face_recognition yêu cầu CMake và dlib (cài trước: pip install cmake dlib)
# - Trên Windows có thể cần Visual C++ Build Tools
# - Có thể điều chỉnh phiên bản nếu gặp lỗi tương thích.
###############################

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Computer Vision & Image Processing
opencv-python-headless>=4.8.0
numpy>=1.24.0

# Face Recognition (sử dụng dlib backend)
face-recognition>=1.3.0
dlib>=19.24.0

# Deep Learning - Anti-Spoofing Model
torch>=2.0.0
torchvision>=0.15.0

# Database
pymongo>=4.7.0
python-dotenv>=1.0.0

# JSON Processing
pydantic>=2.0.0


